<UserControl x:Class="BuildVisualizer.ToolWindow.BuildVisualizerToolWindowControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
			 xmlns:vm="clr-namespace:BuildVisualizer.ViewModels"
			 xmlns:views="clr-namespace:BuildVisualizer.Views"
			 xmlns:converters="clr-namespace:BuildVisualizer.Converters"
			 Background="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"
			 Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
			 mc:Ignorable="d"
			 d:DesignHeight="300" d:DesignWidth="300"
			 d:DataContext="{d:DesignInstance Type=vm:BuildVisualizerViewModel}"
			 Name="MyToolWindow">
	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
		<converters:InverseBooleanToVisibilityConverter x:Key="BoolToVisibilityInverseConverter"/>
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,10,10,10">
			<Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="5" Margin="0,0,5,0"/>
			<Button Content="{Binding ToggleViewText}" Command="{Binding ToggleViewCommand}" Padding="5"/>
		</StackPanel>

		<!-- List View -->
		<ListBox Grid.Row="1" Margin="10,0,10,10"
				 ItemsSource="{Binding Projects}"
				 Visibility="{Binding IsGraphView, Converter={StaticResource BoolToVisibilityInverseConverter}}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Vertical" Margin="0,5,0,5">
						<StackPanel Orientation="Horizontal">
							<Border Width="16" Height="16" Background="{Binding StatusColor}" Margin="0,0,5,0"/>
							<TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
							<TextBlock Text="{Binding Status}" Margin="5,0,0,0" FontSize="10" Foreground="Gray" VerticalAlignment="Center"/>
						</StackPanel>
						<TextBlock Text="{Binding DependenciesText}" FontSize="10" Foreground="Gray" Margin="20,2,0,0"/>
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

		<!-- Graph View -->
		<ScrollViewer Grid.Row="2" Margin="10,0,10,10"
					  HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
					  Visibility="{Binding IsGraphView, Converter={StaticResource BoolToVisibilityConverter}}">
			<ItemsControl ItemsSource="{Binding GraphRowGroups}" Margin="10">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel Orientation="Vertical"/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Border Background="{Binding BackgroundBrush}"
								BorderBrush="Transparent"
								BorderThickness="0"
								CornerRadius="5"
								Margin="0,0,0,10"
								Padding="8">
							<ItemsControl ItemsSource="{Binding Nodes}">
								<ItemsControl.ItemsPanel>
									<ItemsPanelTemplate>
										<WrapPanel Orientation="Horizontal"/>
									</ItemsPanelTemplate>
								</ItemsControl.ItemsPanel>
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<views:ProjectNodeControl Margin="5"/>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</Border>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>
	</Grid>
</UserControl>
