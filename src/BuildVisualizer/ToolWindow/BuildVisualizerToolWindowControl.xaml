<UserControl x:Class="BuildVisualizer.ToolWindow.BuildVisualizerToolWindowControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
			 xmlns:vm="clr-namespace:BuildVisualizer.ViewModels"
			 xmlns:views="clr-namespace:BuildVisualizer.Views"
			 xmlns:converters="clr-namespace:BuildVisualizer.Converters"
			 Background="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"
			 Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
			 mc:Ignorable="d"
			 d:DesignHeight="300" d:DesignWidth="300"
			 d:DataContext="{d:DesignInstance Type=vm:BuildVisualizerViewModel}"
			 Name="MyToolWindow">
	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
		<converters:InverseBooleanToVisibilityConverter x:Key="BoolToVisibilityInverseConverter"/>
		<converters:NullableTimeSpanConverter x:Key="NullableTimeSpanConverter"/>

		<!-- Column header templates for sort indicators -->
		<DataTemplate x:Key="SortableHeaderTemplate">
			<TextBlock Text="{Binding}" HorizontalAlignment="Left"/>
		</DataTemplate>
		<DataTemplate x:Key="SortableHeaderAscTemplate">
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="{Binding}"/>
				<TextBlock Text=" ▲" FontSize="9" VerticalAlignment="Center"/>
			</StackPanel>
		</DataTemplate>
		<DataTemplate x:Key="SortableHeaderDescTemplate">
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="{Binding}"/>
				<TextBlock Text=" ▼" FontSize="9" VerticalAlignment="Center"/>
			</StackPanel>
		</DataTemplate>
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,10,10,10">
			<Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="5" Margin="0,0,5,0"/>
			<Button Content="{Binding ToggleViewText}" Command="{Binding ToggleViewCommand}" Padding="5"/>
		</StackPanel>

		<!-- List View -->
		<ListView Grid.Row="1" Margin="10,0,10,10"
				  ItemsSource="{Binding SortedProjects}"
				  Visibility="{Binding IsGraphView, Converter={StaticResource BoolToVisibilityInverseConverter}}"
				  GridViewColumnHeader.Click="ListViewHeader_Click">
			<ListView.View>
				<GridView>
					<GridViewColumn HeaderTemplate="{StaticResource SortableHeaderTemplate}" Header="Project Name" Width="200">
						<GridViewColumn.HeaderContainerStyle>
							<Style TargetType="GridViewColumnHeader">
								<Setter Property="Tag" Value="Name"/>
							</Style>
						</GridViewColumn.HeaderContainerStyle>
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"/>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn HeaderTemplate="{StaticResource SortableHeaderTemplate}" Header="Build Status" Width="110">
						<GridViewColumn.HeaderContainerStyle>
							<Style TargetType="GridViewColumnHeader">
								<Setter Property="Tag" Value="Status"/>
							</Style>
						</GridViewColumn.HeaderContainerStyle>
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<Border Background="{Binding StatusColor}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left">
									<TextBlock Text="{Binding Status}" Foreground="{Binding StatusTextColor}" FontSize="11" FontWeight="SemiBold"/>
								</Border>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn HeaderTemplate="{StaticResource SortableHeaderTemplate}" Header="Build Start" Width="80">
						<GridViewColumn.HeaderContainerStyle>
							<Style TargetType="GridViewColumnHeader">
								<Setter Property="Tag" Value="BuildStart"/>
							</Style>
						</GridViewColumn.HeaderContainerStyle>
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding BuildStart, TargetNullValue='', StringFormat=HH:mm:ss}"/>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn HeaderTemplate="{StaticResource SortableHeaderTemplate}" Header="Build Stop" Width="80">
						<GridViewColumn.HeaderContainerStyle>
							<Style TargetType="GridViewColumnHeader">
								<Setter Property="Tag" Value="BuildStop"/>
							</Style>
						</GridViewColumn.HeaderContainerStyle>
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding BuildStop, TargetNullValue='', StringFormat=HH:mm:ss}"/>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn HeaderTemplate="{StaticResource SortableHeaderTemplate}" Header="Duration" Width="80">
						<GridViewColumn.HeaderContainerStyle>
							<Style TargetType="GridViewColumnHeader">
								<Setter Property="Tag" Value="BuildDuration"/>
							</Style>
						</GridViewColumn.HeaderContainerStyle>
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding BuildDuration, Converter={StaticResource NullableTimeSpanConverter}}"/>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn HeaderTemplate="{StaticResource SortableHeaderTemplate}" Header="Project Type" Width="90">
						<GridViewColumn.HeaderContainerStyle>
							<Style TargetType="GridViewColumnHeader">
								<Setter Property="Tag" Value="ProjectType"/>
							</Style>
						</GridViewColumn.HeaderContainerStyle>
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding ProjectType}" TextTrimming="CharacterEllipsis"/>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn HeaderTemplate="{StaticResource SortableHeaderTemplate}" Header="Dependencies" Width="200">
						<GridViewColumn.HeaderContainerStyle>
							<Style TargetType="GridViewColumnHeader">
								<Setter Property="Tag" Value="DependenciesText"/>
							</Style>
						</GridViewColumn.HeaderContainerStyle>
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding DependenciesText}" TextTrimming="CharacterEllipsis" Foreground="Gray" FontSize="11"/>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
				</GridView>
			</ListView.View>
		</ListView>

		<!-- Graph View -->
		<ScrollViewer Grid.Row="2" Margin="10,0,10,10"
					  HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
					  Visibility="{Binding IsGraphView, Converter={StaticResource BoolToVisibilityConverter}}">
			<ItemsControl ItemsSource="{Binding GraphRowGroups}" Margin="10">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel Orientation="Vertical"/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Border Background="{Binding BackgroundBrush}"
								BorderBrush="Transparent"
								BorderThickness="0"
								CornerRadius="5"
								Margin="0,0,0,10"
								Padding="8">
							<ItemsControl ItemsSource="{Binding Nodes}">
								<ItemsControl.ItemsPanel>
									<ItemsPanelTemplate>
										<WrapPanel Orientation="Horizontal"/>
									</ItemsPanelTemplate>
								</ItemsControl.ItemsPanel>
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<views:ProjectNodeControl Margin="5"/>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</Border>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>
	</Grid>
</UserControl>
